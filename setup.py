from setuptools import setup, find_packages

setup(
    name="geometrylib",
    version="0.1",
    packages=find_packages(),
    description="Библиотека для расчёта площади геометрических фигур",
    author="Vlkazmin",
)
